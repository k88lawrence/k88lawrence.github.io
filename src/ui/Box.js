import styled from '@emotion/styled'
import { space, layout, color, border, position } from 'styled-system'

const Box = styled.div`
  ${space}
  ${layout}
  ${border}
  ${position}
  ${color}
`

export default Box